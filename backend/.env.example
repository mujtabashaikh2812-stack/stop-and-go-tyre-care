# MongoDB Atlas connection string
# Replace with your actual cluster credentials before deploying
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/stop_and_go_tyre_care?retryWrites=true&w=majority

# Server port
PORT=5000

# Frontend origin for CORS (update for production)
CLIENT_ORIGIN=http://localhost:5173
